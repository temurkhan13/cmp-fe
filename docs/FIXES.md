# CMP Frontend — Fixes Log

Tracks all fixes applied during testing, with the feature area, issue, and resolution.

---

| # | Date | Feature | Issue | Fix Applied |
|---|------|---------|-------|-------------|
| 1 | 2026-04-14 | Subscriptions & Billing | Frontend calling `/api/subscription` but backend serves at `/api/stripe/subscription` — resulting in 404 | Updated API paths in `PlanAndBilling.jsx` from `/subscription` to `/stripe/subscription` for GET (fetch plans) and POST (initiate checkout) endpoints |
| 2 | 2026-04-14 | Subscriptions & Billing | `POST /api/stripe/subscription` response was returning `{ sessionId }` but frontend expects `{ redirectToCheckoutURL }` — checkout redirect never triggered | Fixed `createSubscription` controller in `Subscription/controller.js` to return `{ status: true, redirectToCheckoutURL }` instead of `{ sessionId }` |
| 3 | 2026-04-14 | Subscriptions & Billing | No `/success` page existed — Stripe redirected to a 404 after payment | **FE:** Created `PaymentSuccess.jsx` with loading/success/error states, registered `/success` route in `plainRoutesData.js`, exported from `modules/auth/index.js` |
| 4 | 2026-04-14 | Subscriptions & Billing | No backend endpoint to verify Stripe checkout session — subscription update relied solely on webhooks | **BE:** Added `POST /api/stripe/subscription/verify-session` endpoint — retrieves session from Stripe, checks `payment_status === "paid"`, then upserts `user_subscriptions` table using existing `handleSubscriptionEvent` logic. Added route, controller, and service in `Subscription/` module |
| 5 | 2026-04-14 | Subscriptions & Billing | Success page was a placeholder with a timeout — not connected to the verify endpoint | **FE:** Wired `PaymentSuccess.jsx` to call `POST /api/stripe/subscription/verify-session` with `session_id` from URL, shows success/error based on response |
| 6 | 2026-04-14 | Subscriptions & Billing | User had to manually type email on Stripe checkout page | **BE:** Added `customer_email: user.email` to `stripe.checkout.sessions.create()` in `Subscription/service.js` so the email is pre-filled |
| 7 | 2026-04-14 | Subscriptions & Billing | "Upgrade Plan" button in sidebar shown to all users, even those with an active subscription | **FE:** Added `useSelector` for `auth.user` in `DashboardLayout.jsx` and wrapped the upgrade CTA in `{user && !user.subscription && (...)}` so it only renders for non-subscribed users |
| 8 | 2026-04-14 | Subscriptions & Billing | `currentPlanName` fallback showed "Free" even when no subscription data was loaded yet | **FE:** Changed fallback in `PlanAndBilling.jsx` from `'Free'` to `'-'` so the UI doesn't mislead while data is loading |
| 9 | 2026-04-14 | Landing Page | `HeroSection.jsx` used HTML `class` attribute instead of React's `className` — caused console warnings | **FE:** Replaced all `class=` with `className=` in `HeroSection.jsx` |
| 10 | 2026-04-14 | Subscriptions & Billing | Typo in `modules/auth/index.js` — comment said `// stipe` instead of `// stripe` | **FE:** Fixed typo to `// stripe` |
| 11 | 2026-04-14 | AI Assistant — Chat List | No loading indicator when switching folders — chat list appeared empty during fetch | **FE:** Added `chatsLoading` state and `<Spinner />` in `NewChat.jsx`; set loading true on folder switch and initial load, cleared in `.finally()` |
| 12 | 2026-04-14 | AI Assistant — Chat List | Chat items broke when backend returned `id` instead of `_id`, or `chat_title` instead of `chatTitle` | **FE:** Added `normalizeChats()` in `NewChat.jsx` to map `_id`/`id` and `chatTitle`/`chat_title`/`title` uniformly before dispatching to Redux |
| 13 | 2026-04-14 | AI Assistant — Chat Navigation | Chat selection used `window.history.replaceState` — React Router was unaware of the URL change | **FE:** Replaced with `navigate('/assistant/chat/${chatId}', { replace: true })` in both `NewChat.jsx` and `MessagesSection.jsx` |
| 14 | 2026-04-14 | AI Assistant — New Chat | After sending the first message in a new chat, user had to wait for full chat list refresh before the chat appeared selected | **FE:** In `MessagesSection.jsx`, immediately dispatch `setCurrentChatId` and navigate to the new chat URL from the create-chat response, before refreshing the chat list |
| 15 | 2026-04-14 | AI Assistant — Chat Loading | Selecting a chat showed stale content from the previous chat while the new one was loading | **FE:** In `MessagesSection.jsx`, added `isChatLoading`/`isChatFetching` guards — shows `<ScaleLoader />` until the correct chat data arrives |
| 16 | 2026-04-14 | AI Assistant — Messages | `message.sender` field was `undefined` because backend returns `from` instead of `sender` | **BE:** Added `sender: m.from` mapping in `getAssistantChat` in `workSpaces/service.js`. **FE:** Also added fallback `(message.sender \|\| message.from)` checks in `MessagesSection.jsx` |
| 17 | 2026-04-14 | AI Assistant — Avatars | User and AI avatars used raw `<img>` with hardcoded external fallback URLs (`avatar.iran.liara.run`, `via.placeholder.com`) | **FE:** Replaced with `<UserAvatar>` component in `MessagesSection.jsx` — uses `src` prop with `name`-based initials fallback, no external URLs |
| 18 | 2026-04-14 | AI Assistant — User Profile | `userProfilePhoto` fetch was unnecessarily async and didn't extract user name for avatar fallback | **FE:** Simplified to synchronous `localStorage.getItem` in `MessagesSection.jsx`, added `userName` state derived from `firstName`/`lastName`/`name`/`email` |
| 19 | 2026-04-14 | AI Assistant — Bookmarks | Bookmarks in `AssistantSidebar.jsx` used `currentChat` (stale) and crashed on `undefined` bookmarks/messages | **FE:** Switched to `chatData`, added null-safe guards, handled both `_id`/`id` and `messageId`/`message_id` field names, filtered null results with `.filter(Boolean)` |
| 20 | 2026-04-14 | AI Assistant — Bookmarks | `ChatBookmark.jsx` showed nothing (not even "no data" message) when bookmarks array was empty | **FE:** Changed condition from `!messages` to `!messages \|\| messages.length === 0`, fixed message text to plural "bookmarks" |
| 21 | 2026-04-14 | AI Assistant — Comments | Comment edit, delete, reply operations all missing `messageId` in API URL — resulting in 404s | **FE:** Passed `messageId` (with `message_id` fallback) through all comment/reply CRUD calls in `Comments.jsx`. **FE:** Updated `workspaceApi.js` — `updateComment`, `removeComment`, `addReply`, `updateReply`, `removeReply` endpoints now include `/message/:messageId/` in the URL path |
| 22 | 2026-04-14 | AI Assistant — Comments | `removeComment` and `removeReply` API URLs used plural path segments (`/folders/`, `/chats/`, `/comments/`, `/replies/`) but backend serves singular (`/folder/`, `/chat/`, `/comment/`, `/reply/`) | **FE:** Fixed all plural→singular URL segments in `workspaceApi.js` for `removeChat`, `removeComment`, `updateReply`, `removeReply` |
| 23 | 2026-04-14 | AI Assistant — Comments | Reply edit/delete dispatched local Redux actions (`removeReply`, `updateReply`) instead of calling the API — changes were lost on refresh | **FE:** Wired `useUpdateReplyMutation` and `useRemoveReplyMutation` in `Comments.jsx` to call the backend API |
| 24 | 2026-04-14 | AI Assistant — Comments | Reply deletion had no backend endpoint | **BE:** Added `deleteReplyFromComment` service, controller, and `DELETE /:workspaceId/folder/:folderId/chat/:chatId/message/:messageId/comment/:commentId/reply/:replyId` route in `workSpaces/` module |
| 25 | 2026-04-14 | AI Assistant — Comments | Reply `updateReply` used `PUT` method but backend expects `PATCH` | **FE:** Changed method from `PUT` to `PATCH` in `workspaceApi.js` |
| 26 | 2026-04-14 | AI Assistant — Comments | Shared `editedReplyText` state meant typing a reply in one comment affected all others | **FE:** Replaced single `editedReplyText` with per-comment `replyTexts` object keyed by `commentId` in `Comments.jsx` |
| 27 | 2026-04-14 | AI Assistant — Comments | Comment search input was non-functional — no filtering or state wiring | **FE:** Added `searchQuery` state, wired the search `<input>`, and filter `filteredComments` by text and user name in `Comments.jsx` |
| 28 | 2026-04-14 | AI Assistant — Comments | "Sort by date" dropdown selection had no effect on comment order | **FE:** Added sort logic in `filteredComments` — sorts by `timestamp`/`created_at` descending when "Sort by date" is selected |
| 29 | 2026-04-14 | AI Assistant — Comments | Dropdown menus for edit/delete on comments and replies stayed open after clicking an action | **FE:** Added `dropdownKey` state and `closeDropdown()` — increments key to force re-render/close on each action click |
| 30 | 2026-04-14 | AI Assistant — Comments | `folderId` in `Comments.jsx` read from `state.workspaces.currentFolderId` which was not being set | **FE:** Switched to `selectSelectedFolder` selector and derived `folderId` as `selectedFolder?._id \|\| selectedFolder?.id` |
| 31 | 2026-04-14 | AI Assistant — Cache | Comment, reply, and bookmark mutations didn't invalidate the chat cache — stale data shown until manual refresh | **FE:** Added `tagTypes: ['Workspace', 'Chat']` to `workspaceApi.js` and `invalidatesTags` on `addComment`, `updateComment`, `removeComment`, `addReply`, `updateReply`, `removeReply`, `addBookmark`, `removeBookmark` mutations |
| 32 | 2026-04-14 | AI Assistant — Chat Delete | "Move to Trash" in `NewChatSidebarModal` dispatched a local Redux action instead of calling the API — chat reappeared on refresh | **FE:** Replaced `dispatch(deleteChat())` with `useRemoveChatMutation` API call, added `workspaceId`/`folderId`/`onDeleted` props. Cleaned up commented-out code |
| 33 | 2026-04-14 | AI Assistant — Chat Delete | `removeChat` API URL used plural segments and had a trailing slash — 404 | **FE:** Fixed URL in `workspaceApi.js` from `workspace/.../folders/.../chats/.../` to `workspace/.../folder/.../chat/...` |
| 34 | 2026-04-14 | AI Assistant — Chat Delete | After deleting the currently active chat, the UI still showed the deleted chat's messages | **FE:** Added `onDeleted` callback in `NewChat.jsx` — filters deleted chat from Redux state, and if it was the active chat, clears `currentChatId` and navigates to `/assistant/chat` |
| 35 | 2026-04-14 | Assessment — Like/Dislike/Bookmark | Like, dislike, and bookmark buttons in `MessagesSection.jsx` only toggled local cosmetic state (`setReactions('like')`) — never called the API mutations | **FE:** Wired `onClick` handlers to `handleLikeClick`, `handleDislikeMessage`, and `handleAddBookmark` which call `useLikeChatMessageMutation`, `useDislikeChatMessageMutation`, `useAddBookmarkMutation`/`useRemoveBookmarkMutation` |
| 36 | 2026-04-14 | Assessment — Like/Dislike/Bookmark | `folderId` was a plain string but handlers accessed `folderId._id \|\| folderId.id` — always resolved to `undefined` | **FE:** Passed `folderId` directly (already a string) in all three handlers |
| 37 | 2026-04-14 | Assessment — Like/Dislike/Bookmark | Handlers used `chatId` from `state.workspaces.currentChatId` (AI Assistant context) instead of the assessment's own ID | **FE:** Replaced `chatId` with `assessmentId` in like/dislike/bookmark mutation calls, removed unused `chatId` selector |
| 38 | 2026-04-14 | Assessment — Like/Dislike/Bookmark | Single `reactions` and `bookmark` state shared across all messages — clicking one highlighted all | **FE:** Changed `reactions`, `copy`, and `bookmark` state from strings to per-message objects keyed by message ID |
| 39 | 2026-04-14 | Dashboard — Assessments | Dashboard showed "No Assessments associated with this workspace" even when assessments existed — `getFolderEntities` queried `folder_assessments` table but the `WorkspaceAssessments` module creates records in `workspace_assessments` table | **BE:** Changed `getFolderEntities` in `workSpaces/service.js` to query `workspace_assessments` instead of `folder_assessments`, removed `is_soft_deleted` filter (that table uses hard deletes) |
| 40 | 2026-04-15 | Dashboard — Project List | Creating a new project didn't refresh the project list — `handleDataUpdated` dispatched `fetchDashboardStats()` but never updated the `activeWorkspace` local state with the fresh data | **FE:** Changed `handleDataUpdated` in `dashboardHomeComponents/index.jsx` to `await` the stats fetch and update `activeWorkspace` local state with the refreshed workspace matching the current workspace ID |
| 41 | 2026-04-15 | Settings — Change Password | `POST /api/auth/change-password` returned 404 — route, controller, and validation were never wired up despite service function existing | **BE:** Added `changePassword` controller in `users/controller.js` (maps `currentPassword` → `oldPassword`, gets `email` from `req.user`), added `POST /change-password` route in `users/route.js` with `auth()` middleware, updated validation in `users/validation.js` to accept `{ currentPassword, newPassword }` instead of `{ email, oldPassword, newPassword }` |
| 42 | 2026-04-15 | Settings — Delete Account | No backend endpoint for account deletion — `DELETE /api/auth/delete-account` returned 404 | **BE:** Added `deleteAccount` controller in `users/controller.js` (calls `userService.deleteUser`), added `DELETE /delete-account` route in `users/route.js` with `auth()` middleware |
| 43 | 2026-04-15 | Settings — Delete Account | After account deletion, redirect went to `/login` (404) instead of `/log-in` | **FE:** Fixed redirect URL in `PersonalInfo.jsx` from `/login` to `/log-in` |
| 44 | 2026-04-15 | Assessment — Version History | `saveVersion` looked for the latest report by scanning messages for text starting with `"## "` — fragile and missed reports not stored as messages | **BE:** Changed `saveVersion` in `Assessments/service.js` to read from `assessment_reports` table instead, using `report.content` as the version snapshot |
| 45 | 2026-04-15 | Assessment — Version History | Version history panel showed versions even when no report had been generated yet — confusing UX | **FE:** In `AssessmentVersionHistory.jsx`, added `hasReport` check by fetching assessment data; shows "No report has been generated yet" message when `report.isGenerated` is falsy |
| 46 | 2026-04-15 | Assessment — Version History | API calls in `AssessmentVersionHistory.jsx` used `${config.apiURL}/assessment/...` (full URL prefix) but `apiClient` already has the base URL — resulting in doubled paths | **FE:** Changed to relative paths (`/assessment/${assessmentId}/versions`, etc.) in all `apiClient` calls |
| 47 | 2026-04-15 | Assessment — Report Editor | Save and Download buttons were inside the Jodit toolbar config — broke when toolbar was re-rendered or collapsed | **FE:** Moved Save and Download (PDF) buttons out of the Jodit toolbar config into standalone `<button>` elements above the editor in `Editor.jsx` |
| 48 | 2026-04-15 | Assessment — Report Editor | No save status feedback — user couldn't tell if save succeeded or failed | **FE:** Added `saveStatus` state (`idle`/`saving`/`saved`/`error`) in `Editor.jsx` — Save button shows "Saving...", "Saved", or "Save Failed" with color feedback |
| 49 | 2026-04-15 | Assessment — Report Editor | `Editor.defaultProps` used deprecated React pattern causing console warning | **FE:** Replaced `Editor.defaultProps = { height: '100%' }` with default parameter `height = '100%'` in function signature |
| 50 | 2026-04-15 | Assessment — Report Export | `docx`, `pptxgenjs`, and `exceljs` dynamic imports used `.default` but some bundler configurations expose them differently — causing `undefined` constructor errors | **FE:** Changed to `const mod = await import(...)` then `mod.default \|\| mod` pattern for all three export libraries in `Editor.jsx` |
| 51 | 2026-04-15 | Assessment — Report Export | `WordReportTemplate.defaultProps` used deprecated React pattern | **FE:** Replaced `WordReportTemplate.defaultProps = { content: '' }` with default parameter `content = ''` in function signature |
| 52 | 2026-04-15 | Assessment — Chat Messages | Assessment chat displayed all messages as AI (left-aligned) — no distinction between user messages, AI messages, and Q&A format | **FE:** Rewrote message rendering in assessment `MessagesSection.jsx` to handle three formats: `role === 'user'` (right-aligned with user avatar), `role === 'ai'` (left-aligned with AI avatar), and legacy Q&A format (`!item.role` with question left / answer right) |
| 53 | 2026-04-15 | Assessment — Chat Messages | User avatar in assessment chat used raw `<img>` with `photoPath` and `getInitials()` fallback — inconsistent with AI Assistant | **FE:** Replaced with `<UserAvatar>` component using `userProfilePhoto`/`userName` state (same pattern as AI Assistant `MessagesSection.jsx`) |
| 54 | 2026-04-15 | Assessment — Media Sidebar | Media sidebar in assessment view showed hardcoded empty data — not connected to actual chat content | **FE:** Added `onMediaUpdate` callback prop to assessment `MessagesSection.jsx` that extracts images, documents, and links from chat messages; passed `chatMedia` to `Assessments.jsx` via `Chat.jsx` |
| 55 | 2026-04-15 | Assessment — Questionnaire | `workspaceId` and `folderId` in `Questionnaire.jsx` read from `state.workspaces.currentWorkspaceId`/`currentFolderId` which were stale or unset | **FE:** Switched to `selectWorkspace` and `selectSelectedFolder` selectors, derived IDs as `currentWorkspace?.id` and `selectedFolder?._id \|\| selectedFolder?.id` |
| 56 | 2026-04-15 | Assessment — Questionnaire | Submitting the questionnaire with all questions unanswered silently succeeded — sent empty survey | **FE:** Added validation in `handleSubmit` in `Questionnaire.jsx` — checks `hasAtLeastOneAnswer`, shows error message "Please answer at least one question before submitting" |
| 57 | 2026-04-15 | AI Assistant — Folder Mismatch | Switching workspaces kept the previous workspace's `selectedFolder` in Redux — chat list fetched chats from the wrong folder | **FE:** In `NewChat.jsx`, added `folderBelongsToWorkspace` guard that checks `currentWorkspace.folders` before fetching chats. In `MessagesSection.jsx`, added `folderId` IIFE that validates `selectedFolder` against `currentWorkspace.folders` and falls back to active/first folder |
| 58 | 2026-04-15 | AI Assistant — Folder State | `setSelectedFolder` reducer set `currentFolderId = action.payload._id` — failed when folder objects had `id` instead of `_id` | **FE:** Changed to `action.payload?._id \|\| action.payload?.id \|\| null` in `workspacesSlice.js` |
| 59 | 2026-04-15 | Dashboard — Trash | Trash operations (move, restore, delete) for assessments queried `folder_assessments` table but data lives in `workspace_assessments` | **BE:** Changed `moveEntityToTrash`, `restoreEntityFromTrash`, `deleteEntityFromTrash`, and `getUserTrash` in `workSpaces/service.js` to use `workspace_assessments` table for assessment entity type |
| 60 | 2026-04-15 | Dashboard — Trash | Trash items had no deletion date — UI showed empty date columns | **BE:** Added `updated_at` to select queries for workspaces, folders, chats, and assessments in `getUserTrash`; added `dateDeleted` field (formatted via `toLocaleDateString()`) to all returned trash items |
| 61 | 2026-04-15 | Dashboard — My Assessments | `getUserAssessments` queried `folder_assessments` table — returned empty results | **BE:** Changed to query `workspace_assessments` table with `is_soft_deleted: false` filter in `workSpaces/service.js` |
| 62 | 2026-04-15 | Dashboard — Entity Lists | `getUserChats`, `getUserSitemaps`, `getUserAssessments`, and `getUserWireframes` threw `ApiError(NOT_FOUND, "No workspaces found")` when user had no folders — crashed the page instead of showing empty state | **BE:** Changed all four functions to `return []` instead of throwing when no folders exist |
| 63 | 2026-04-15 | Digital Playbook — Stages Order | Playbook stages always sorted by `created_at` only — dragging to reorder had no effect since there was no `sort_order` column support | **BE:** In `getFullPlaybook`, added primary sort by `sort_order` with `created_at` fallback. In `simpleUpdate`, added handler for `body.stageOrder` array that updates `sort_order` for each stage ID. Both have graceful fallbacks if `sort_order` column doesn't exist yet |
| 64 | 2026-04-15 | Digital Playbook — List | Playbook list loaded all playbooks at once — no pagination | **FE:** Added `page`, `totalPages`, `totalResults` state and `ITEMS_PER_PAGE = 9` in `Playbook-List.jsx`. `fetchPlaybooks` now passes `?page=&limit=` params. Added Previous/Next pagination controls with ellipsis for large page counts |
| 65 | 2026-04-15 | Digital Playbook — List Layout | Playbook cards used flexbox with `maxWidth: 350px` and manual margins — inconsistent spacing and alignment | **FE:** Changed to CSS Grid with `gridTemplateColumns: 'repeat(3, 1fr)'` and `gap: '1rem'` in `Playbook-List.jsx` |
| 66 | 2026-04-15 | Digital Playbook — Delete | Deleting a playbook removed it from local state only (`setPlaybooks(prev => prev.filter(...))`) — if it was the last item on a page, the page stayed empty | **FE:** After delete, re-fetches from server; if the deleted item was the last on the current page, navigates to the previous page |
| 67 | 2026-04-15 | Dashboard — Folder Delete | `handleProceedMoveToTrash` in `FileStructure.jsx` used stale `folderId.id` from outer scope instead of the clicked folder's ID | **FE:** Changed to accept `folder` parameter, uses `folder.id \|\| folder._id`; closes dropdown instead of modal on success |
| 68 | 2026-04-15 | Dashboard — Workspace Delete | `handleMoveToTrash` in `Workspaces.jsx` used `activeWorkspace.id` — always deleted the active workspace instead of the clicked one | **FE:** Changed to accept `workspaceId` parameter, called as `onClick={() => handleMoveToTrash(workspace.id)}` |
| 69 | 2026-04-15 | Auth — Email Validation | Email validation in sign-in and sign-up accepted malformed emails like `user@.com` or `user@domain` that Yup's `.email()` allows | **FE:** Added `.matches(/^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/)` regex validation to both sign-in and sign-up schemas in `validationAuth.js` |
| 70 | 2026-04-15 | Settings — Profile Photo Upload | Profile photo upload saved file to local disk (`public/uploads/`) via `multer.diskStorage` — broke on ephemeral hosting (Render) where filesystem resets on deploy | **BE:** Changed `fileUpload.js` from `multer.diskStorage` to `multer.memoryStorage()`. Added `uploadProfilePhoto` in `users/service.js` that uploads the buffer to Supabase Storage (`avatars` bucket) and returns the public URL. Removed `public/uploads/` directory creation from `app.js` |
| 71 | 2026-04-15 | Settings — Profile Photo Upload | `updateUser` controller set `body.photoPath = file.filename` (local filename) before passing to service — no longer valid with Supabase storage | **BE:** Changed `updateUser` controller to pass `file` object directly to `userService.updateUser(userId, body, file)`. Service now handles upload and sets `photo_path` from Supabase public URL |
| 72 | 2026-04-15 | Settings — Profile Photo Delete | Deleting profile photo only cleared the avatar in UI state — never sent the removal to the server | **FE:** Added `photoDeleted` state in `PersonalInfo.jsx`. When photo is deleted, sets `photoDeleted: true`. On save, sends `removePhoto: true` in form data. **BE:** Service handles `body.photoPath = ''` to clear `photo_path` in database |
| 73 | 2026-04-15 | Settings — Profile Update | `updateUser` sent profile fields and photo in two separate PATCH requests — photo upload didn't include other fields, causing partial updates | **FE:** In `userSlice.js`, changed to send all fields + photo together in a single FormData request when a file is present. Returns full updated user from server instead of optimistically merging |
| 74 | 2026-04-15 | Settings — Profile Update | After saving profile changes, Redux store was updated with manually merged local data (`{ ...currentUser, ...updatedUserData, photoPath: avatar }`) instead of server response — caused stale/incorrect state | **FE:** Changed `PersonalInfo.jsx` to dispatch `updateUser` action with `result.payload` (server response). Added proper `updateUser` reducer in `authSlice.js` that updates `state.user` and syncs `localStorage` |
| 75 | 2026-04-15 | Settings — Profile Update | `updateUser` validation in backend rejected requests with `photoPath: ''` (empty string for photo removal) — Joi schema didn't allow empty strings | **BE:** Added `.allow('')` to `photoPath` field in `users/validation.js` `updateUser` schema |
| 76 | 2026-04-15 | Settings — Profile Update | `updateUser` service returned raw Supabase row (`data`) instead of formatted user object — frontend received `first_name`/`last_name` instead of `firstName`/`lastName` | **BE:** Changed `updateUser` in `users/service.js` to return `formatUser(data)` |
| 77 | 2026-04-15 | Dashboard — Header Avatar | `Header.jsx` read user data from `localStorage` on mount (`useState`/`useEffect`) — never updated when profile was changed in Settings without a page reload | **FE:** Switched to `useSelector((state) => state.auth.user)` for reactive user data, derived `photoPath` from Redux state |
| 78 | 2026-04-15 | UI — Avatar Initials Color | Avatar initials fallback circles used blue (`#007bff` bg / `#ffffff` text) across `Header.jsx`, `AssessmentHeader.jsx`, `Comments.jsx`, and assessment `MessagesSection.jsx` — inconsistent with brand | **FE:** Changed all four to brand colors (`#C3E11D` bg / `#0B1444` text) |
| 79 | 2026-04-15 | AI Assistant — Chat Rename | No ability to rename a chat — only "Move to Trash" option in the chat context menu | **FE:** Added "Rename" option with `<FaPen>` icon in `NewChatSidebarModal.jsx`. Uses `prompt()` for input, calls `useUpdateChatMutation` API. Added `chatTitle`, `onRenamed` props. `NewChat.jsx` passes these props and updates local Redux chat list on rename |
| 80 | 2026-04-15 | AI Assistant — Chat Context Menu | `NewChatSidebarModal` received `folderId` from `selectedFolder` — wrong when chat belonged to a different folder | **FE:** Changed `NewChat.jsx` to pass `chat.folder_id` (from chat data) as primary `folderId`, with `selectedFolder` as fallback |
| 81 | 2026-04-15 | Assessment — Version Restore | Restoring a version re-inserted the messages snapshot but didn't restore the report content — report editor still showed the current version | **BE:** In `restoreVersion` in `Assessments/service.js`, added update to `assessment_reports` table setting `content` to `version.report_content` |
| 82 | 2026-04-15 | Assessment — Version History | `handleSaveVersion` called `fetchVersions()` (undefined after rename) and showed hardcoded error toast | **FE:** Changed to `await fetchData()` and used `err?.response?.data?.message` with fallback for error toast in `AssessmentVersionHistory.jsx` |
| 83 | 2026-04-15 | Dashboard — Workspace Rename | No ability to rename a workspace — only "Delete" option in workspace dropdown menu | **FE:** Added `handleRenameWorkspace` in `Workspaces.jsx` using `useUpdateWorkspaceMutation`. Shows `prompt()` for input, validates min 3 chars, prevents renaming "Default Workspace". Added "Rename" option in dropdown above "Delete" |
